<UserControl x:Class="teammy.AssigneeEllipseTask"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:teammy"
             mc:Ignorable="d" DataContext="{Binding RelativeSource={RelativeSource self}}" Height="20" Width="20">
    <UserControl.Resources>
        <local:ColorToBrushConverter x:Key="ColorConverter"/>
        <local:NameToCharsConverter x:Key="NameConverter"/>
    </UserControl.Resources>
    <Grid x:Name="containerGrid" Width="20" Height="20" ToolTip="{Binding User}">
        <Ellipse x:Name="epsBack" Fill="{Binding BackColor, Converter={StaticResource ColorConverter}}" Height="15" Width="15"/>
        <Label x:Name="lblAssigneeChars" Content="{Binding User, Converter={StaticResource NameConverter}}" Margin="0,5,0,0" FontFamily="Berlin Sans FB" Foreground="White" Padding="0" HorizontalContentAlignment="Center" FontSize="9.5" FontWeight="Bold" />
    </Grid>
</UserControl>
